apiVersion: v2
name: predator-nvidia
description: Helm chart for Predator Analytics on NVIDIA Server (lab-gpu)
type: application
version: 19.0.0
appVersion: "19.0.0"

# No external dependencies - all services defined in templates/