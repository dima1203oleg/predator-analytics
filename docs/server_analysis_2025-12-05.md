# 📊 Комплексний аналіз системи Predator Analytics

**Дата:** 2025-12-05  
**Виконав:** Автоматичний аналіз через SSH  
**Сервер:** dev (5.tcp.eu.ngrok.io:14564)

---

## ✅ Виконані завдання

### 1️⃣ SSH Підключення та ключі
- ✅ Створено новий SSH-ключ: `~/.ssh/id_ed25519_ngrok`
- ✅ Публічний ключ додано до сервера: `~/.ssh/authorized_keys`
- ✅ Налаштовано безпарольний вхід через SSH-ключ
- ✅ Підключення перевірено та працює коректно

**Команда для підключення:**
```bash
ssh -i ~/.ssh/id_ed25519_ngrok dima@5.tcp.eu.ngrok.io -p 14564
```

---

## 🖥️ Характеристики сервера

### Система
- **ОС:** Linux dev 6.14.0-36-generic (Ubuntu 24.04)
- **Архітектура:** x86_64
- **Ядро:** 6.14.0-36-generic #36~24.04.1-Ubuntu SMP PREEMPT_DYNAMIC

### Ресурси
- **Оперативна пам'ять:** 49 GB (використано: 5.2 GB, вільно: 33 GB, буфери: 10 GB)
- **Swap:** 8.0 GB (не використовується)
- **Диск:** 177 GB (використано: 152 GB = 91%, вільно: 16 GB)
- **Boot partition:** 2.0 GB (використано: 197 MB = 11%)

⚠️ **УВАГА:** Основний диск заповнений на 91% - рекомендується звільнити місце!

### Docker контейнери
```
CONTAINER ID   IMAGE                    STATUS                                  PORTS                             
d4c228e238aa   10-predator-app          Restarting (2)                          (немає портів)
6a20d5446a85   10-nexus-frontend        Up About an hour                        0.0.0.0:8082->80/tcp
c9ab17cf06ea   grafana/grafana:latest   Up About an hour                        0.0.0.0:3001->3000/tcp
```

⚠️ **ПРОБЛЕМА:** Контейнер `predator10-app` постійно перезапускається  
**Причина:** Не може знайти файл `/app/main.py` всередині контейнера  
**Помилка:** `python3: can't open file '/app/main.py': [Errno 2] No such file or directory`  
**Рішення:** Перевірити Dockerfile та переконатися, що файли правильно копіюються в контейнер

### K8s/Kubernetes
- ❌ `kubectl` недоступний на сервері
- ✅ Знайдено конфігурації: `~/.kube/` та `~/.minikube/`
- ℹ️ Можливо, потрібно активувати Minikube або підключитися до кластера

---

## 📂 Структура проекту на сервері

### Основні директорії
```
~/predator-analytics/          # Основний проект (64 піддиректорії)
~/PREDATOR13/                  # Версія 13 (40 піддиректорій)
~/предатор 10/                 # Версія 10 (46 піддиректорій)
~/predator_10/                 # Ще одна версія 10
~/predator-analytics-10/       # Analytics версія 10
~/p10_new/                     # Нова версія P10
```

### Компоненти проекту `predator-analytics`

#### Backend
```
./backend/
  ├── api/              # FastAPI роутери
  ├── services/         # Бізнес-логіка
  ├── models/           # ORM моделі
  ├── schemas/          # Pydantic схеми
  ├── agents/           # Автономні агенти
  ├── tasks/            # Celery/фонові завдання
  ├── core/             # Ядро (config, DB, prompts)
  ├── db/               # Сесії та міграції
  └── scripts/          # Скрипти розгортання
```

#### Інфраструктура
```
./infra/
  ├── docker/           # Docker-конфігурації
  ├── kubernetes/       # K8s маніфести
  ├── helm/             # Helm charts
  ├── monitoring/       # Prometheus, Grafana
  ├── qdrant/           # Векторна БД
  └── systemd/          # Systemd сервіси
```

#### Інші компоненти
- `./routers/` - Додаткові роутери
- `./services/` - Сервісна логіка
- `./telegram/`, `./telegram_bot/` - Telegram-інтеграція
- `./scripts/` - Скрипти налаштування
- `./tests/` - Тести
- `./.venv/`, `./venv/` - Віртуальні середовища Python
- `./node_modules/` - Node.js залежності

### Ключові файли
- `requirements.txt` - Python-залежності (оновлено 14.06)
- `package.json` - Node.js залежності
- `docker-compose.yml` - НЕМАЄ (можливо, використовується окремо)
- `README.md` - Документація (оновлено 12.06)

---

## 🔍 Аналіз компонентів

### Python середовище
- ✅ Віртуальні середовища створені (`.venv` та `venv`)
- ✅ `requirements.txt` наявний
- ⚠️ Знайдено логи встановлення: `pip_install.log` (125 KB)

### Node.js
- ✅ `node_modules` встановлено (77 піддиректорій)
- ✅ `package.json` та `package-lock.json` наявні
- ✅ Використовується також `pnpm` (знайдено `pnpm-lock.yaml`)

### Docker
- ✅ Docker працює
- ⚠️ 1 контейнер із проблемами (постійні рестарти)
- ✅ Grafana доступна на порті 3001
- ✅ Frontend доступний на порті 8082

### Бази даних та сервіси
Директорії для:
- **PostgreSQL** (немає явної директорії, можливо, в контейнері)
- **Redis** (визначення: `redis-deployment.yaml`)
- **Qdrant** (директорія `./qdrant/`)
- **Ollama** (директорія `./ollama/`)
- **OpenSearch** (директорія `./opensearch/`)

---

## 🧩 Використання компонентів

### Локальна машина (Mac)
На вашій локальній машині (`/Users/dima-mac/Documents/Predator_21/`):

#### Python сервіси (працюють)
```bash
export PATH="/Users/dima-mac/Library/Python/3.9/bin:$PATH"
cd ua-sources && ...
```
Статус: **RUNNING** (1h56m35s)

#### Frontend (Vite)
```bash
export PATH="$HOME/node/bin:$PATH" && npm run dev
```
Статус: **RUNNING** (1h51m50s)

#### Структура локального проекту
```
/Users/dima-mac/Documents/Predator_21/
├── ua-sources/          # Python backend (FastAPI + Celery)
│   ├── app/
│   │   ├── api/routers/    # Analytics, ETL, Simulation, etc.
│   │   ├── services/       # AI engine, Risk engine, RAG, LLM
│   │   ├── connectors/     # CKAN, Customs, NBU, Prozorro, Tax
│   │   ├── etl/            # ETL процесор
│   │   ├── core/           # Config, DB, Prompts, Celery
│   │   └── models.py       # SQLAlchemy ORM
│   ├── alembic/            # Міграції БД
│   └── requirements.txt
├── frontend/            # React + Vite
├── components/          # React компоненти (20 файлів)
├── views/               # React сторінки (18 файлів)
├── backend/             # Додатковий backend?
├── scripts/             # Bash/Python скрипти (27 файлів)
└── docker-compose.yml   # Docker конфігурація
```

---

## ✅ Перевірка використання компонентів

### Backend роутери (`ua-sources/app/api/routers/`)
| Роутер | Підключені сервіси | Використання |
|--------|-------------------|--------------|
| `analytics.py` | `ai_engine`, `risk_engine` | ✅ Використовується |
| `search.py` | `rag_service` | ✅ Використовується |
| `simulation.py` | `opponent_engine` | ✅ Використовується |
| `etl.py` | `etl_service` | ✅ Використовується |
| `integration.py` | `integration_service` | ⚠️ Можливо не використовується |
| `data.py` | `data_service` | ⚠️ Імпорт без використання |

### Сервіси (`ua-sources/app/services/`)
- `ai_engine.py` - ✅ Використовується в analytics
- `risk_engine.py` - ✅ Використовується в analytics
- `llm.py` - ✅ Базовий LLM-сервіс
- `rag.py` - ✅ Пошук документів
- `opponent_engine.py` - ✅ Симуляції
- `mock_generator.py` - ℹ️ Генерація тестових даних
- `vault_service.py` - ℹ️ Управління секретами

### Коннектори (`ua-sources/app/connectors/`)
- `base.py` - ✅ Базовий клас
- `prozorro.py` - ✅ API Prozorro
- `customs.py`, `ua_customs.py` - ✅ Дані митниці
- `tax.py`, `ua_tax.py` - ✅ Податкові дані
- `nbu_fx.py` - ✅ Курси валют НБУ
- `ckan_generic.py` - ✅ Відкриті дані
- `registry.py` - ✅ Реєстри

---

## 🚨 Виявлені проблеми

### Критичні
1. **Диск заповнений на 91%** - потрібно очистити або розширити
2. **Контейнер `predator10-app` постійно перезапускається** - помилка в коді або конфігурації
3. **Kubectl недоступний** - неможливо керувати Kubernetes-кластером

### Попередження
4. Зайві імпорти в `data.py` та `integration.py`
5. Декілька версій проекту на сервері (може плутати)
6. Немає активного `docker-compose.yml` на сервері (або використовується інший шлях)

---

## 📋 Рекомендації

### Негайні дії
1. **Очистити диск:**
   ```bash
   # Видалити старі Docker образи
   docker system prune -a --volumes
   
   # Очистити APT кеш
   sudo apt clean
   
   # Видалити старі логи
   sudo journalctl --vacuum-time=7d
   ```

2. **Виправити контейнер `predator10-app`:**
   ```bash
   docker logs d4c228e238aa
   docker inspect d4c228e238aa
   ```

3. **Активувати Kubernetes (якщо потрібно):**
   ```bash
   minikube start
   # або
   export KUBECONFIG=~/.kube/config
   ```

### Покращення
4. Видалити зайві імпорти в роутерах
5. Об'єднати або видалити дублюючі версії проекту
6. Налаштувати автоматичне очищення логів та тимчасових файлів
7. Додати моніторинг використання диска (алерти)

---

## 🔐 Безпека

### SSH Ключі
- ✅ Створено новий ED25519 ключ
- ✅ Ключ додано до `authorized_keys`
- ✅ Безпарольний вхід працює

### Існуючі ключі на сервері
Знайдено 8 SSH-ключів у `~/.ssh/authorized_keys`:
- `dima@dev`
- `dima@MacBook-Pro-Dmitro.local` (2 інстанси)
- `your_email@example.com`
- `dima@nvidia-server`
- `dima1203@gmail.com`
- `dima@predator-analytics` (НОВИЙ)
- Один RSA-ключ

⚠️ **Рекомендація:** Видалити невикористовувані ключі та залишити лише актуальні.

---

## 📊 Загальний висновок

### ✅ Що працює
- SSH підключення налаштовано ідеально
- Сервер має достатньо ресурсів (RAM, CPU)
- Docker працює, контейнери запущені
- Grafana та Frontend доступні
- Python та Node.js середовища налаштовані
- Всі основні компоненти наявні та підключені

### ⚠️ Що потребує уваги
- Диск майже повний (91%)
- Один контейнер не працює стабільно
- Kubernetes потребує активації
- Декілька дублюючих версій проекту

### 🎯 Наступні кроки
1. Очистити диск
2. Виправити помилку в `predator10-app`
3. Видалити зайві версії проекту
4. Налаштувати моніторинг
5. Оптимізувати код (видалити зайві імпорти)

---

**Статус:** ✅ Аналіз завершено успішно  
**Підключення:** ✅ Працює без проблем  
**Готовність системи:** 🟡 Потребує дрібних доопрацювань
