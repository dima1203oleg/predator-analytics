# 🔄 Робота з сервером Predator Analytics

Цей документ описує, як працювати з віддаленим сервером та переключатися між Mac і сервером.

## 📍 Параметри підключення

| Параметр | Значення |
|----------|----------|
| **IP** | 194.177.1.240 |
| **Port** | 6666 |
| **User** | dima |
| **Password** | Dima@1203 |
| **Директорія** | ~/predator-analytics |

---

## 📋 Зміст

1. [Швидкий старт](#швидкий-старт)
2. [Скрипти](#скрипти)
3. [Workflow](#workflow)
4. [Резервне копіювання](#резервне-копіювання)
5. [Troubleshooting](#troubleshooting)

---

## 🚀 Швидкий старт

### Підключення до сервера

```bash
# Простий спосіб (використовуючи скрипт)
./scripts/server-connect.sh

# Або вручну
ssh -p 6666 dima@194.177.1.240
```

### Перевірка статусу сервера

```bash
./scripts/server-status.sh
```

### Синхронізація коду

```bash
# На сервер (з Mac)
./scripts/sync-to-server.sh

# З сервера (на Mac - резервне копіювання)
./scripts/sync-from-server.sh
```

---

## 🛠️ Скрипти

У директорії `scripts/` є корисні скрипти для роботи з сервером:

### 1. `server-connect.sh` - Підключення до сервера

**Використання:**
```bash
# Інтерактивне підключення
./scripts/server-connect.sh

# Виконати команду на сервері
./scripts/server-connect.sh "ls -la"
./scripts/server-connect.sh "docker ps"
./scripts/server-connect.sh "docker compose logs predator_backend"
```

### 2. `server-status.sh` - Перевірка статусу

**Показує:**
- ✅ Стан підключення
- 🎮 GPU інформацію (nvidia-smi)
- 💻 Системну інформацію
- 💾 Використання диска
- 🧠 Використання пам'яті
- 🐳 Docker контейнери
- 🔌 Відкриті порти

**Використання:**
```bash
./scripts/server-status.sh
```

### 3. `sync-to-server.sh` - Синхронізація на сервер

**Копіює код з Mac на сервер, виключаючи:**
- `node_modules/`
- `.venv/`, `venv/`
- `__pycache__/`, `*.pyc`
- `.git/`
- Логи та тимчасові файли

**Використання:**
```bash
# Синхронізація
./scripts/sync-to-server.sh

# Перегляд змін без копіювання (dry-run)
./scripts/sync-to-server.sh --dry-run
```

### 4. `sync-from-server.sh` - Резервне копіювання

**Копіює код з сервера на Mac в директорію `server-backup/`**

**Використання:**
```bash
# Резервне копіювання
./scripts/sync-from-server.sh

# Перегляд змін без копіювання (dry-run)
./scripts/sync-from-server.sh --dry-run
```

---

## 📝 Workflow

### Типовий робочий процес

#### 1️⃣ **Ранок - Підключення до сервера**

```bash
# Перевірити статус
./scripts/server-status.sh

# Підключитися
./scripts/server-connect.sh

# На сервері
cd ~/predator-analytics
git pull origin main
docker compose restart
```

#### 2️⃣ **Робота на сервері**

```bash
# Перевірити сервіси
docker compose ps

# Переглянути логи backend
docker compose logs -f predator_backend

# Переглянути логи orchestrator
docker compose logs -f predator_orchestrator

# Моніторинг GPU
watch -n 1 nvidia-smi
```

#### 3️⃣ **Якщо пропало світло - переключення на Mac**

```bash
# 1. Резервне копіювання з сервера
./scripts/sync-from-server.sh

# 2. Продовжити роботу локально на Mac
cd /Users/dima-mac/Documents/Predator_21
./start_local.sh  # або docker compose up -d
```

#### 4️⃣ **Світло з'явилося - повернення на сервер**

```bash
# 1. Синхронізувати зміни на сервер
./scripts/sync-to-server.sh

# 2. Підключитися до сервера
./scripts/server-connect.sh

# 3. Перезапустити сервіси на сервері
cd ~/predator-analytics
git add . && git commit -m "sync" && git push
docker compose restart
```

---

## 💾 Резервне копіювання

### Автоматичне резервне копіювання (опціонально)

Створіть cron job для автоматичної синхронізації:

```bash
# Відкрити crontab
crontab -e

# Додати рядок (бекап кожні 2 години)
0 */2 * * * /Users/dima-mac/Documents/Predator_21/scripts/sync-from-server.sh >> /Users/dima-mac/Documents/Predator_21/logs/sync.log 2>&1
```

### Ручне резервне копіювання

```bash
# Створити повний бекап з датою
./scripts/sync-from-server.sh
cd /Users/dima-mac/Documents/Predator_21
tar -czf "backup-$(date +%Y%m%d-%H%M%S).tar.gz" server-backup/
```

---

## 🔧 Troubleshooting

### Проблема: Сервер не відповідає

```bash
# Перевірити підключення
ping 194.177.1.240

# Перевірити порт
nc -zv 194.177.1.240 6666

# Спробувати підключитися з verbose
ssh -vvv -p 6666 dima@194.177.1.240
```

### Проблема: Синхронізація не працює

```bash
# Перевірити SSH-ключ
ls -la ~/.ssh/id_ed25519_ngrok*

# Перевірити права доступу
chmod 600 ~/.ssh/id_ed25519_ngrok
chmod 644 ~/.ssh/id_ed25519_ngrok.pub

# Спробувати dry-run
./scripts/sync-to-server.sh --dry-run
```

### Проблема: Недостатньо місця на диску сервера

```bash
# Підключитися до сервера
./scripts/server-connect.sh

# Очистити Docker
docker system prune -af --volumes

# Очистити APT кеш
sudo apt clean
sudo apt autoclean

# Очистити логи
sudo journalctl --vacuum-time=7d

# Видалити старі Python кеші
find . -type d -name "__pycache__" -exec rm -rf {} +
find . -type f -name "*.pyc" -delete
```

### Проблема: Контейнер не запускається

```bash
# Перевірити логи
./scripts/server-connect.sh "docker logs predator_backend --tail 100"

# Перевірити Docker Compose
./scripts/server-connect.sh "cd ~/predator-analytics && docker compose ps"

# Перезапустити контейнери
./scripts/server-connect.sh "cd ~/predator-analytics && docker compose restart"
```

---

## 📌 Корисні команди

### На Mac

```bash
# Швидка перевірка статусу
./scripts/server-status.sh

# Підключення та виконання команди
./scripts/server-connect.sh "htop"

# Синхронізація + підключення
./scripts/sync-to-server.sh && ./scripts/server-connect.sh
```

### На сервері

```bash
# Перевірити всі сервіси
docker compose ps

# Переглянути логи
docker compose logs -f

# Моніторинг ресурсів
htop
docker stats
nvidia-smi -l 1
```

---

## 🎯 Best Practices

1. **Завжди робіть бекап перед великими змінами:**
   ```bash
   ./scripts/sync-from-server.sh
   ```

2. **Використовуйте Git для контролю версій:**
   ```bash
   git add .
   git commit -m "Опис змін"
   git push origin main
   ```

3. **Регулярно перевіряйте статус сервера:**
   ```bash
   ./scripts/server-status.sh
   ```

4. **Тримайте локальну копію актуальною:**
   ```bash
   ./scripts/sync-from-server.sh  # Щодня
   ```

5. **Очищуйте старі файли:**
   ```bash
   # На сервері
   docker system prune -f
   find . -name "*.log" -mtime +7 -delete
   ```

---

**Останнє оновлення:** 2025-12-14
**Версія:** 2.0 (Static IP)
