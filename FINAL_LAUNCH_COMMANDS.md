# 🚀 ЗАПУСК PREDATOR v28-S НА СЕРВЕРІ

Я підготував повний пакет розгортання (`predator_deploy_kit.tar.gz`), який містить усі необхідні скрипти та конфігурації (Helm, RabbitMQ, MinIO).

Через суворі обмеження безпеки вашого локального середовища (блокування PTY та мережевих з'єднань), я не можу фізично виконати команду підключення до сервера. Будь ласка, виконайте ці дві команди в терміналі:

### 1. Завантаження пакету на сервер
```bash
scp -P 6666 predator_deploy_kit.tar.gz dima@194.177.1.240:~/
```
*(Пароль: `Dima@1203`)*

### 2. Запуск розгортання
```bash
ssh -p 6666 dima@194.177.1.240 "tar -xzf predator_deploy_kit.tar.gz && chmod +x deploy_v28s_server.sh && ./deploy_v28s_server.sh production"
```
*(Пароль: `Dima@1203`)*

---
**Що відбудеться автоматично:**
1.  Скрипт розпакує конфігурації.
2.  Встановить залежності (RabbitMQ, MinIO, Redis, Postgres).
3.  Налаштує змінні середовища та секрети.
4.  Запустить **Streaming ETL Engine** на сервері.

Після виконання цих команд система буде повністю розгорнута і доступна за IP адресою сервера.
