
# CONTRIBUTING.md — Predator Analytics

Дякуємо, що хочеш допомогти розвивати Predator Analytics 💿🚀  
Цей файл описує базові правила та очікування для внеску в код, документацію та інфраструктуру.

---

## 1. Формати версій та релізів

- Версії нумеруються як `vMajor.Minor.Patch`, фокус зараз на `Major.Minor`.
- Історія версій описана в [`CHANGELOG.md`](./CHANGELOG.md).
- Детальні технічні зміни — в:
  - `docs/RELEASE_NOTES_v20.0.md`
  - `docs/RELEASE_NOTES_v21.0.md`
  - `docs/ROADMAP_v21.0.md`

Поточний фокус:
- **v20.0 — Singularity Edition** (стабільна реалізація Mini Stack 2–6 + Admin UI).
- **v21.0 — LoRA Trainer & SaaS Hardening** (запроєктовано, готується до імплементації).

---

## 2. Як створювати issues

Перш ніж створювати issue:

1. **Перевір, чи проблема вже не заведена** в існуючих issues.
2. Вибери відповідний шаблон:
   - 🐞 Bug Report — для багів і падінь.
   - 💡 Feature Request — для нових фіч або покращень.

У баг-репорті обов’язково вкажи:
- `env: mac / nvidia / oracle`
- кроки відтворення
- очікувану / фактичну поведінку
- логи / скріншоти, якщо є
- чи перевіряв(-ла) через `tests/smoke-admin.http`

---

## 3. Git-воркфлоу та гілки

Рекомендований варіант:

- `main` — стабільна гілка (останній реліз, напр. v20.0).
- `dev` (або аналог) — інтеграційна гілка для наступної версії (напр. v21.0).
- Фічі — в окремих гілках:
  - `feature/lora-trainer-job`
  - `feature/admin-filters`
  - `fix/rate-limit-bug` тощо.

### Базовий цикл роботи

1. Відгалузитись від актуальної гілки (`main` або `dev`):

   ```bash
   git checkout dev
   git pull
   git checkout -b feature/my-cool-change
   ```

2. Зробити зміни (код / Helm / доки).
3. Запустити тести (див. розділ 6).
4. Закомітити з осмисленим повідомленням.
5. Відкрити Pull Request, прив’язавши його до issue (якщо є).

---

## 4. Стиль комітів

Рекомендований (наближено до conventional commits):
- `feat: ...` — новий функціонал
- `fix: ...` — виправлення багу
- `docs: ...` — документація
- `chore: ...` — дрібні зміни, що не впливають на логіку
- `test: ...` — зміни в тестах
- `infra: ...` — інфраструктура (Helm, ArgoCD, CI/CD, k8s)

Приклади:
- `feat: add LoRA trainer job v1.0`
- `fix: handle 429 errors in admin dashboard`
- `docs: add release notes for v21.0`
- `infra: enable lora-trainer in nvidia env`

---

## 5. Локальне середовище (env: mac)

### 5.1. Передумови

На локальній машині бажано мати:
- Docker або локальний Kubernetes (minikube / k3d / k3s).
- `kubectl`, `helm`.
- python (3.10+ або 3.11+).
- node (18+) + npm / yarn.
- `make` (опційно).
- Редактор (VS Code / PyCharm / інший) з плагіном для HTTP-запитів (VS Code REST Client рекомендується).

### 5.2. Базові кроки (орієнтовно)

1. Підняти базові сервіси (PostgreSQL, Redis, Keycloak, Vault, MinIO тощо).
2. Запустити сервіс `predator-brain`.
3. Запустити сервіс `ua-sources`.
4. Запустити frontend (Dashboard + Admin UI).
5. Прогнати `tests/smoke-admin.http` з `@env = mac`.

Конкретні команди та скрипти залежать від того, як організований твій docker-compose / Helm-чарти.

---

## 6. Тести

Перед тим як відкривати PR:

1. **Backend-тести**
   - Запусти юніт/інтеграційні тести бекенду (якщо налаштовані, напр. pytest).
2. **Frontend-тести**
   - Запусти базові тести фронту (напр. `npm test` / `npm run lint`).
3. **Smoke-тести admin API**
   - Відкрий `tests/smoke-admin.http` у VS Code (REST Client):
   - налаштуй `@env` та Keycloak креденшли;
   - виконай:
     - `login`
     - `council_run`
     - `etl_upload` + `etl_import_status`
     - `admin_api_usage`
     - `admin_training_samples`
     - `council_feedback_positive`.
   - Переконайся, що критичні ендпоінти відповідають коректно.

Якщо змінюєш білінг / rate limiting / self-learning — бажано додати або оновити відповідні тести.

---

## 7. Зміни в інфраструктурі (Helm / ArgoCD / k8s)

Якщо змінюєш:
- Helm-чарти (`charts/...`),
- umbrella-чарт `charts/predator`,
- ArgoCD env-файли (`k8s/argocd/envs/*.yaml`),

будь ласка:
1. Онови відповідну документацію, якщо це впливає на деплой:
   - `docs/PREPROD_CHECKLIST.md`
   - `docs/RELEASE_NOTES_v20.0.md` або `docs/RELEASE_NOTES_v21.0.md`
2. Чітко вкажи в PR:
   - які саме env зачеплені (`mac` / `nvidia` / `oracle`);
   - чи потрібні додаткові кроки (міграції БД, оновлення секретів тощо).

---

## 8. Дока та реліз-ноти

Кожна суттєва фіча бажано повинна:
- мати згадку в:
  - `CHANGELOG.md`,
  - відповідних `docs/RELEASE_NOTES_*.md`,
- якщо це частина roadmap — бути прив’язаною до:
  - `docs/ROADMAP_v21.0.md`,
  - або іншого актуального roadmap-файлу.

---

## 9. Кодекс спільноти (людською мовою)

- Будь поважним / поважною до інших учасників.
- Не публікуй приватні ключі, токени, персональні дані.
- Будь чесним(-ою) щодо стану фічі (якщо “draft” — так і напиши).
- Якщо щось ламається — описуй факти, а не людей 😊

---

## 10. Питання?

Якщо не впевнений(-а), з чого почати:
- Подивись відкриті issues з тегами `good first issue` / `help wanted`.
- Орієнтуйся на релізи:
  - **v20.0** — стабілізація Mini Stack.
  - **v21.0** — LoRA Trainer + SaaS Hardening.

Ласкаво просимо в Predator Analytics 🧠🚀
