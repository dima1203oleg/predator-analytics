
# Predator Analytics v18.6

**Repository:** `Predator_analytics_19`

## 🚀 GitOps Architecture

Цей репозиторій структуровано для автоматичного розгортання через ArgoCD та GitHub Actions.

### Структура
*   `frontend/` - React додаток (Vite + TypeScript)
*   `ua-sources/` - Backend сервіс (Python FastAPI ETL)
*   `charts/` - Helm чарти для Kubernetes
*   `scripts/` - Утиліти для синхронізації та деплою

### Швидкий Старт

1.  **Синхронізація:**
    ```bash
    ./scripts/sync_to_github.sh
    ```

2.  **Локальний Запуск (Docker Compose):**
    ```bash
    docker-compose up --build
    ```

3.  **Міграція (якщо структура папок стара):**
    ```bash
    ./scripts/migrate_structure.sh
    ```

## 🛡️ Truth-Only Protocol
Система працює виключно з перевіреними даними. Симуляції та галюцинації AI заборонені в `production` середовищі.
