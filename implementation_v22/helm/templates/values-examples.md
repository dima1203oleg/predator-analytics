# Notes for values files

Placeholders / recommended settings:

- `global.env` — dev/staging/prod
- `autoPromote.enabled` — boolean to turn on/off self-improvement autopromotion
- `inference.gpu.enabled` — depends on env
- For production, use Kubernetes secrets for API keys and other credentials
