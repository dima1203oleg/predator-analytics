# 📁 Scripts Directory

Скрипти для роботи з Predator Analytics.

## 🔐 Підключення до сервера

| Скрипт | Опис |
|--------|------|
| `server-connect.sh` | Інтерактивне SSH підключення до сервера |
| `server-status.sh` | Перевірка статусу сервера (GPU, Docker, RAM) |
| `connect-server.expect` | Автоматичне підключення з паролем |
| `run-remote-cmd.expect` | Виконання однієї команди на сервері |
| `ssh_connect_password.exp` | SSH підключення з автоматичним паролем |

## 📦 Синхронізація

| Скрипт | Опис |
|--------|------|
| `sync-to-server.sh` | Rsync коду з Mac на сервер |
| `sync-from-server.sh` | Бекап коду з сервера на Mac |
| `git_deploy.sh` | GitOps деплой через git pull |

## 🚀 Деплой

| Скрипт | Опис |
|--------|------|
| `deploy_orchestrator.sh` | Деплой Orchestrator |
| `deploy_telegram_bot_v4.sh` | Деплой Telegram Bot v4 |
| `deploy_fix_v4.exp` | Швидкий фікс та рестарт бота |

## 🌐 Веб-сервіси

| Скрипт | Опис |
|--------|------|
| `server-start-web.sh` | Запуск веб-інтерфейсів |
| `check_remote_status.sh` | Швидка перевірка Docker |

## ⚙️ Конфігурація

| Файл | Опис |
|------|------|
| `recommended_ssh_config` | Рекомендований SSH config |

---

## 📍 Параметри серверу

```
IP: 194.177.1.240
Port: 6666
User: dima
Directory: ~/predator-analytics
```

## 💡 Швидкий старт

```bash
# Перевірити статус
./scripts/server-status.sh

# Підключитись
./scripts/server-connect.sh

# Синхронізувати код
./scripts/sync-to-server.sh
```
