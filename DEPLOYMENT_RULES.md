# 📋 Правила Розгортання Predator Analytics

## 🎯 Основні Правила

### 1. Пріоритет Серверів
```
┌─────────────────────────────────────────────────────────────┐
│  🥇 ПРІОРИТЕТ 1: NVIDIA Server (168.119.232.81)            │
│     - Основний сервер для розгортання                       │
│     - GPU для ML/AI навантаження                            │
│     - Kubernetes (K3s) кластер                              │
│     - Повний production стек                                │
│                                                             │
│  🥈 ПРІОРИТЕТ 2: MacBook (локально)                        │
│     - ТІЛЬКИ якщо NVIDIA сервер недоступний                │
│     - Для development та тестування                         │
│     - Збереження ресурсів для програмування                │
└─────────────────────────────────────────────────────────────┘
```

### 2. Коли використовувати Mac
- ❌ Для деплою: тільки якщо NVIDIA сервер недоступний (ngrok не працює/мережа/обслуговування)
- ✅ Локальна розробка і тестування нових фіч без production-навантаження
- ✅ Налагодження коду або інтеграцій
- 🔄 Після відновлення доступності NVIDIA сервера повертаємо деплой туди

### 3. Перевірка доступності сервера
```bash
# Через ngrok (динамічний порт)
ssh -o ConnectTimeout=5 dev-ngrok "echo ok" 2>/dev/null && echo "✅ Server OK" || echo "❌ Server DOWN"

# Через прямий IP (якщо порт відкритий)
nc -zv 168.119.232.81 22 -w 5
```

## 🔄 Автоматичний Fallback

При кожному запиті на деплой:
1. Перевірити доступність NVIDIA сервера (ngrok або прямий порт)
2. Якщо доступний → деплоїмо тільки туди, Mac не використовуємо
3. Якщо недоступний → повідомляємо + пропонуємо локальний запуск (dev/test)
4. Після відновлення сервера повертаємо деплой на NVIDIA

## 📍 Конфігурація Серверів

### NVIDIA Server
- **IP**: 168.119.232.81
- **SSH**: через ngrok (динамічний порт)
- **Kubernetes**: K3s
- **GPU**: NVIDIA RTX/Tesla
- **OS**: Ubuntu 22.04

### MacBook Pro
- **Hostname**: MacBook-Pro-Dmitro
- **Docker**: Docker Desktop
- **OS**: macOS
- **Usage**: Development/testing; без production навантаження

## ⚠️ Важливо!
- Production деплой і навантаження → тільки NVIDIA сервер
- Mac для локальної розробки/налагодження або як тимчасовий fallback
- Зберігати ресурси Mac для програмування

---
*Оновлено: 8 грудня 2025*
