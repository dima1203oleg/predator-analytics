apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "predator-umbrella.fullname" . }}-ollama-models
  labels:
    {{- include "predator-umbrella.labels" . | nindent 4 }}
    app.kubernetes.io/component: ollama
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
