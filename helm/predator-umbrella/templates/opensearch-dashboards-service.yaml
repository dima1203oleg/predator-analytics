apiVersion: v1
kind: Service
metadata:
  name: {{ include "predator-umbrella.fullname" . }}-dashboards
  labels:
    {{- include "predator-umbrella.labels" . | nindent 4 }}
    app.kubernetes.io/component: dashboards
spec:
  type: ClusterIP
  ports:
    - port: 5601
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "predator-umbrella.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: dashboards
