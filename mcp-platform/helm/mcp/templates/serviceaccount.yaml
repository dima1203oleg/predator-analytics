{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "mcp-platform.serviceAccountName" . }}
  labels:
    {{- include "mcp-platform.labels" . | nindent 4 }}
{{- end }}
