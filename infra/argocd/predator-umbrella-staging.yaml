apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: predator-umbrella-staging
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "https://github.com/dima1203oleg/predator-analytics.git"
    targetRevision: main
    path: helm/predator-umbrella
    helm:
      valueFiles:
        - values-staging.yaml
  destination:
    server: "https://kubernetes.default.svc"
    namespace: predator-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
