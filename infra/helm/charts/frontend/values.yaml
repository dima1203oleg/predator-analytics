enabled: true
replicaCount: 1
image:
  repository: "ghcr.io/dima1203oleg/predator-frontend"
  tag: "latest"
  pullPolicy: IfNotPresent
service:
  type: ClusterIP
  port: 80
config:
  apiBaseUrl: "/api/v1"

resources:
  requests:
    cpu: 5m
    memory: 32Mi
  limits:
    cpu: 5m
    memory: 32Mi
