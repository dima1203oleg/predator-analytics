{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "rtb-engine.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector: {{- include "rtb-engine.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
{{- end }}
