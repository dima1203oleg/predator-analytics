{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcp-router.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector: {{- include "mcp-router.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
{{- end }}
