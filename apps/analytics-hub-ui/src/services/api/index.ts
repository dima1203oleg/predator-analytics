import { factoryApi } from "./factory";

export const monitoringApi = { getMetrics: async () => ({}) };
export const systemApi = { getStatus: async () => ({}) };
export const apiClient = { get: async () => ({}), post: async () => ({}) };

export { factoryApi };
