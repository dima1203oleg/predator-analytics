events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # =====================================
    # КРИТИЧНО: Налаштування для великих файлів (митні декларації до 1GB)
    # =====================================
    client_max_body_size 1G;
    client_body_timeout 600s;
    client_body_buffer_size 128k;

    # Логування
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    # Оптимізація
    sendfile on;
    keepalive_timeout 120;
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    # Основний сервер
    server {
        listen 80;
        server_name localhost;

        # Статичні файли (фронтенд)
        location / {
            root /usr/share/nginx/html;
            index index.html index.htm;
            try_files $uri $uri/ /index.html;
        }

        # Проксі до бекенду
        location /api/ {
            client_max_body_size 2G;
            proxy_request_buffering off;
            proxy_pass http://backend:8000;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            # Збільшені таймаути для великих файлів та довгих операцій
            proxy_read_timeout 600s;
            proxy_send_timeout 600s;
            proxy_connect_timeout 120s;
        }

        # WebSocket підтримка для Omniscience
        location /api/v25/ws/ {
            proxy_pass http://backend:8000;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_read_timeout 86400;
        }

        # Статичні файли бекенду
        location /static/ {
            proxy_pass http://backend:8000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        # Health check endpoint
        location /health {
            return 200 'OK';
            add_header Content-Type text/plain;
        }

        # OpenSearch Dashboards proxy для вбудованої аналітики
        location /opensearch-dashboards/ {
            proxy_pass http://predator_dashboards:5601/;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_buffering off;
            proxy_read_timeout 300s;
        }

        # Grafana proxy для вбудованих дешбордів
        location /grafana/ {
            proxy_pass http://predator_grafana:3000/;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }

        # Prometheus proxy для метрик
        location /prometheus/ {
            proxy_pass http://predator_prometheus:9090/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        # H2O LLM Studio proxy для ML fine-tuning
        # NOTE: Використовує resolver для graceful handling якщо сервіс недоступний
        location /llm-studio/ {
            resolver 127.0.0.11 valid=30s ipv6=off;
            set $llm_upstream h2o-llmstudio:10101;
            proxy_pass http://$llm_upstream/;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_buffering off;
            proxy_read_timeout 600s;
            client_max_body_size 1G;
            # Fallback error handling
            proxy_intercept_errors on;
            error_page 502 503 504 = @llm_unavailable;
        }

        location @llm_unavailable {
            default_type application/json;
            return 503 '{"error": "H2O LLM Studio сервіс тимчасово недоступний", "status": 503}';
        }

        # MinIO Console proxy для завантаження датасетів
        location /minio/ {
            proxy_pass http://predator_minio:9001/;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_read_timeout 600s;
            client_max_body_size 1G;
        }

        error_page 500 502 503 504 /50x.html;
        location = /50x.html {
            root /usr/share/nginx/html;
        }
    }
}
