apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "predator.fullname" . }}-pdb
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "predator.selectorLabels" . | nindent 6 }}
