*** Begin Patch
*** Update File: deploy/litellm/docker-compose-router.yml
@@
-  ultra-router:
-    build:
-      context: .
-      dockerfile: Dockerfile
-    container_name: ultra-router-v55.3
-    ports:
-      - "4000:4000"
-    environment:
-      # 🚀 GROQ API KEYS (10 шт)
-      GROQ_API_KEY_1: "${GROQ_API_KEY_1:-gsk_test_1}"
-      GROQ_API_KEY_2: "${GROQ_API_KEY_2:-gsk_test_2}"
-      GROQ_API_KEY_3: "${GROQ_API_KEY_3:-gsk_test_3}"
-      GROQ_API_KEY_4: "${GROQ_API_KEY_4:-gsk_test_4}"
-      GROQ_API_KEY_5: "${GROQ_API_KEY_5:-gsk_test_5}"
-      
-      # 📚 HUGGINGFACE
-      HF_API_KEY: "${HF_API_KEY:-hf_test_key}"
-      
-      # 🤝 TOGETHER AI
-      TOGETHER_API_KEY: "${TOGETHER_API_KEY:-together_test_key}"
-      
-      # 🖥️ OLLAMA
-      OLLAMA_API_URL: "http://194.177.1.240:11434"
-      
-      # 📊 LOGGING
-      LOG_LEVEL: "INFO"
-      PYTHONUNBUFFERED: "1"
-    
-    volumes:
-      - ./logs:/app/logs
-      - ./config-stateless.yaml:/app/config.yaml:ro
-    
-    restart: unless-stopped
-    healthcheck:
-      test: ["CMD", "curl", "-f", "http://localhost:4000/health"]
-      interval: 10s
-      timeout: 5s
-      retries: 3
-      start_period: 15s
-    
-    networks:
-      - predator-network
+  ultra-router:
+    image: ghcr.io/berriai/litellm:main-stable
+    container_name: ultra-router-v55.3
+    ports:
+      - "4000:4000"
+    environment:
+      # Master key для Antigravity / Ultra-Router
+      LITELLM_MASTER_KEY: "${LITELLM_MASTER_KEY:-sk-antigravity-master-2026}"
+
+      # Провайдери (безплатні моделі 2026)
+      MISTRAL_API_KEY: "${MISTRAL_API_KEY:-mistral_YOUR_KEY}"
+      DEEPSEEK_API_KEY: "${DEEPSEEK_API_KEY:-deepseek_YOUR_KEY}"
+      GEMINI_API_KEY: "${GEMINI_API_KEY:-gemini_YOUR_KEY}"
+      GROQ_API_KEY: "${GROQ_API_KEY:-groq_YOUR_KEY}"
+
+      # Збережені ключі
+      HF_API_KEY: "${HF_API_KEY:-hf_test_key}"
+      TOGETHER_API_KEY: "${TOGETHER_API_KEY:-together_test_key}"
+
+      # Logging
+      LOG_LEVEL: "INFO"
+      PYTHONUNBUFFERED: "1"
+
+    volumes:
+      - ./logs:/app/logs
+      - ./config-antigravity.yaml:/app/config.yaml:ro
+
+    restart: unless-stopped
+    healthcheck:
+      test: ["CMD", "curl", "-f", "http://localhost:4000/health"]
+      interval: 10s
+      timeout: 5s
+      retries: 3
+      start_period: 15s
+
+    networks:
+      - predator-network
*** End Patch

*** Begin Patch
*** Update File: services/core-api/.env.remote
@@
 TOGETHER_API_KEY="b27_YOUR_API_KEY_HERE"
 
 # ===========================================
 # LOCAL OLLAMA (резерв на Маці)
@@
-# ===========================================
-# LOCAL OLLAMA (резерв на Маці)
-# ===========================================
-# Якщо на Маці запущено: ollama serve
-OLLAMA_LOCAL_ENABLED=false
-OLLAMA_LOCAL_MODELS="llama2,mistral,neural-chat"
+# Antigravity / Ultra-Router master key (used by Antigravity setup)
+LITELLM_MASTER_KEY="sk-antigravity-master-2026"
+
+# Additional provider API keys (безплатні моделі 2026)
+MISTRAL_API_KEY="mistral_YOUR_KEY"
+DEEPSEEK_API_KEY="deepseek_YOUR_KEY"
+GEMINI_API_KEY="gemini_YOUR_KEY"
+GROQ_API_KEY="groq_YOUR_KEY"
+
+# ===========================================
+# LOCAL OLLAMA (резерв на Маці)
+# ===========================================
+# Якщо на Маці запущено: ollama serve
+OLLAMA_LOCAL_ENABLED=false
+OLLAMA_LOCAL_MODELS="llama2,mistral,neural-chat"
*** End Patch
