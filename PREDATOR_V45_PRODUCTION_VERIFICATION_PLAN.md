# 📋 ПЛАН ПОВНОЇ ПРОДУКТИВНОЇ ВЕРИФІКАЦІЇ ТА ДООПРАЦЮВАННЯ PREDATOR V45

Цей документ є планом дій для досягнення цілі: **ПОВНИЙ ПРОДАКШН**. Він базується на аналізі всього діалогу та канонічного ТЗ v45.

---

## 1. АНАЛІЗ СТАНУ ТА РЕЗУЛЬТАТІВ ДІАЛОГУ

### ✅ Вже реалізовано (Верифіковано):
- **Деплой v45**: Server-side build успішний.
- **Гарячі фікси**:
    - Виправлено парсер H2O (JSON Robustness).
    - Виправлено імпорти `app.libs` (Сумісність середовищ).
    - Виправлено Nginx Proxy (`/api/` prefix).
- **Зв'язність**: Бекенд бачить Redis та Ollama.
- **ТЗ**: Сформовано "Єдине джерело істини".

### 🚩 Потребує перевірки/доопрацювання:
- **Інші БД**: Чи бачить бекенд Neo4j, OpenSearch, Qdrant, MinIO? (Connectivity Check).
- **Excel Pipeline**: Чи реально проходить файл `Березень_2024.xlsx` через ВСІ стадії (від Upload до Vectorized)?
- **Graph DB**: Чи завантажуються реальні вузли `Company`, `Product` тощо?
- **Повна українізація**: Чи не залишилось hardcoded English у компонентах `CustomsIntelligenceView.tsx` чи `DataIngestionHub.tsx`?
- **Component Enforcement**: Чи UI реально відображає `used: true` для працюючих компонентів?

---

## 2. ПЛАН ПЕРЕВІРКИ (VERIFICATION STEPS)

### Крок 1: ТЕСТ ІНФРАСТРУКТУРНОЇ ПІДВЕДЕНОСТІ
- [ ] Перевірити PostgreSQL (виконання `SELECT 1`).
- [ ] Перевірити Neo4j (Cypher запит).
- [ ] Перевірити OpenSearch (cluster health).
- [ ] Перевірити Qdrant (collections list).
- [ ] Перевірити MinIO (bucket list).

### Крок 2: E2E ТЕСТ ОСНОВНОГО ПАЙПЛАЙНУ
- [ ] Завантажити `Березень_2024.xlsx`.
- [ ] Моніторити Redis-статуси (FSM).
- [ ] Перевірити наявність нових записів у PostgreSQL.
- [ ] Перевірити генерацію графів у Neo4j.
- [ ] Перевірити індекси в OpenSearch.

### Крок 3: АУДИТ ТА ВИПРАВЛЕННЯ UI
- [ ] Сканування всього `apps/predator-analytics-ui/src` на `[\w]*[a-zA-Z\s]+[\w]*` у текстових вузлах.
- [ ] Переклад знайдених англійських рядків.
- [ ] Перевірка доступності Dashboard (Premium).

### Крок 4: ML/AI ВЕРИФІКАЦІЯ
- [ ] Перевірка успішності циклу самовдосконалення (AZR).
- [ ] Перевірка валідності відповідей LLM (українською).

---

## 3. ПЛАН ТЕСТІВ НА ВИКОНАВЧІСТЬ (PROD PERFORMANCE)

| Тест | Критерій успіху |
| :--- | :--- |
| **Ingestion Speed** | > 1000 рядків/хв для Excel. |
| **Search Latency** | < 200мс для OpenSearch/Qdrant. |
| **H2O Training** | Цикл завершується < 5хв для 10к записів. |
| **Proxy Reliability** | 0% 404/502 помилок при переході по розділах UI. |

---

## 4. ГРАФІК ВИКОНАННЯ (EXECUTION ACTION)

1. **Зараз**: Запуск скрипта діагностики всіх БД.
2. **Далі**: Моделювання живого пайплайну.
3. **Фінал**: Тотальна українізація коду.

---

**Ціль**: Predator Analytics v45 - READY FOR PRODUCTION.
