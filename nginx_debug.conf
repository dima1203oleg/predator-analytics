server {
    listen 80;
    server_name localhost;

    location ^~ /api/v1/som/ {
        return 200 "MATCHED_SOM_LOCATION\n";
    }

    location /api/ {
        return 200 "MATCHED_API_LOCATION\n";
    }

    location / {
        return 200 "MATCHED_ROOT\n";
    }
}
